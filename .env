# ── BACKEND API URL ──────────────────────────────────
# Local: http://localhost:5000
# Production (Vercel): replace this value in Vercel's Environment Variables with your deployed Render backend URL
VITE_API_URL=http://localhost:5000
